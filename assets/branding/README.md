# Branding Assets

Place final branding images in this folder with fixed names:

- `logo.png`
- `framework.png`

Current files are placeholders used to keep docs links stable.
Replace them with the provided high-resolution assets.
