version: "3.9"

services:
  easyshift-api:
    image: python:3.11-slim
    working_dir: /app
    volumes:
      - ./:/app
    command: sh -c "pip install -e . && uvicorn easyshift_maas.api.app:app --host 0.0.0.0 --port 8000"
    ports:
      - "8000:8000"
